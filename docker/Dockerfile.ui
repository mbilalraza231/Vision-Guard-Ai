# ============================================================
# VisionGuard AI - Debug UI Dockerfile
# ============================================================
# TEMPORARY debug interface - READ-ONLY access.
# This is NOT a production service.
# ============================================================

FROM python:3.11-slim

LABEL maintainer="VisionGuard AI"
LABEL description="Debug UI (Streamlit) - TEMPORARY"
LABEL version="1.0.0"
LABEL debug="true"

# Environment
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PYTHONPATH="/app" \
    REDIS_HOST="redis" \
    REDIS_PORT="6379" \
    STREAMLIT_SERVER_PORT="8501" \
    STREAMLIT_SERVER_HEADLESS="true"

# Install minimal deps
RUN apt-get update && apt-get install -y --no-install-recommends \
    fonts-dejavu-core \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir streamlit redis psycopg2-binary

# Copy UI code
COPY debug_ui/ /app/debug_ui/

# Expose Streamlit port
EXPOSE 8501

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=10s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8501/_stcore/health')" || exit 1

# Run Streamlit
CMD ["streamlit", "run", "debug_ui/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
